{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "cache": true,
      "inputs": ["$TURBO_DEFAULT$", ".helios/generated/**"],
      "outputs": [".helios/next/**", "!.helios/next/cache/**"]
    }
  }
}
