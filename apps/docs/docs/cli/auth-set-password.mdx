---
title: helios auth set-password
sidebar_label: auth set-password
description: Resets the password for an existing user account.
---

`yarn helios auth set-password` updates the stored bcrypt hash for the specified user.

## Usage

```bash
yarn helios auth set-password --email "<user@email>" --password "<newPassword>"
```

## Behavior

- Looks up the user by email.
- Hashes the supplied password with `bcryptjs` (cost 10).
- Persists the new hash and confirms the user.
- Prints a success message such as `✅ Password updated successfully for user: user@example.com`.

## Troubleshooting

- **User not found** – verify the email address, or list users with [`helios auth list-users`](./auth-list-users).
- **Weak passwords** – the CLI enforces the configured password policy. Update `HELIOS_PASSWORD_MIN_LENGTH`, `HELIOS_PASSWORD_REQUIRE_DIGIT`, `HELIOS_PASSWORD_REQUIRE_UPPERCASE`, and `HELIOS_PASSWORD_REQUIRE_SPECIAL` if you need different rules.
- **Reused passwords** – hashing is idempotent; rerunning the command with the same password is safe.
