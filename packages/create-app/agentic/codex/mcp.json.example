{
  "mcpServers": {
    "helios": {
      "command": "yarn",
      "args": ["helios", "mcp:dev"],
      "env": {
        "APP_URL": "http://localhost:3000",
        "MCP_API_KEY": "${MCP_API_KEY}"
      }
    }
  }
}
