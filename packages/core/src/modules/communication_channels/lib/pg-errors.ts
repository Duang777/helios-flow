/**
 * @deprecated Import `isUniqueViolation` from `@helios/shared/lib/db/pg-errors` instead.
 * Kept as a re-export so existing hub call sites keep working.
 */
export { isUniqueViolation } from '@helios/shared/lib/db/pg-errors'
